<head>
    <style>
        img{
            width: 28px;
            height: 23px;
        }
        #sign-p > span{
            
            margin-top: 0px;
            display: flex;
            align-items: flex-end;
        }
        #sign-p >span span{
            margin-top: 4px;
        }

    </style>
</head>
<div>

    <p>
        Hi <%= expressThoughts.thought.user.name %>,
    </p>
    <br>
    <p>Your <%= expressThoughts.type %> <strong>
        <% if(expressThoughts.type!="reply") {%>
            <%= expressThoughts.thought.content %>
        <% } else {%>
            <% if(!expressThoughts.thought.isReply) {%>
                <%= expressThoughts.thought.content %>
            <% } else { %>
                <%= expressThoughts.thought.content.content %>
            <% } %>
        <% } %>
    </strong> on the <%= expressThoughts.parentType %> <strong><%= expressThoughts.parentContent %></strong> just got published on AMIGOBOOK.</p>
    <br>
    <p>Thanks for being an active member!</p>
    <br>
    <p id="sign-p">With best regards,<br>
        <span> <span>AMIGOBOOK Team</span> <img src="https://pngimg.com/uploads/peace_symbol/peace_symbol_PNG8.png"></span>
     </p>

</div>